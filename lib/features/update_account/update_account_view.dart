/*
import 'dart:io';

import 'package:flutter/material.dart';
import 'package:untitled/features/update_account/view/widget/update_account_view_body.dart';
class UpdateAccountView extends StatelessWidget {
  final int? currentPageByIndex;
  const UpdateAccountView({Key? key, this.currentPageByIndex}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return WillPopScope(
      onWillPop: ()async{
        return exit(0);
      },
      child: SafeArea(
        child: Scaffold(
          body: UpdateAccountViewBody(),
        ),
      ),
    );
  }
}
*/
